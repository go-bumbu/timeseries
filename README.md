# timeseries
Timeseries is a simple library to store time related data on an RDB using Gorm.

Note: it is not intended for high performance time operations, but rather for those cases where you only
want to handle hundreds of values and using a standalone TSDB is overkill.
